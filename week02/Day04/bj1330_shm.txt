A,B = map(int, input().split())

## 백준에서 말하는 제한은
## 백준 채점기가 입력을 -10000~10000 내에서만 준다는 뜻임
## 이렇게 예외처리 안해도 됨
if A >= -10000 and B <= 10000:
	if	A > B:
		print(">")
	elif A < B:
		print("<")
	else:
		print("==")

# 변수명 정할 때 무슨 목적인지 알아볼 수 있도록 정해줍시다.